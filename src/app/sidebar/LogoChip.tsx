export default function LogoChip() {
    return (
        <div className="grid h-7 w-7 flex-shrink-0 place-items-center rounded-lg bg-[linear-gradient(135deg,var(--magenta),var(--amber))] font-display text-[15px] font-extrabold text-[var(--logo-ink)] shadow-[var(--glow-m)]">
            N
        </div>
    );
}
